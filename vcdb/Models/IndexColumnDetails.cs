namespace vcdb.Models
{
    public class IndexColumnDetails
    {
        /// <summary>
        /// Whether the column should be sorted in descending order in the index
        /// </summary>
        public bool Descending { get; set; }
    }
}
